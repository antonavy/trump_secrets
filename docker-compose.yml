services:
  trump-feed:
    build: .
    container_name: trump-feed
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/${DB_FILENAME:-trump_posts_db.json}
    volumes:
      - ./data:/app/data
